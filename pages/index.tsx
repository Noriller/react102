import { RepoLink } from '../src/RepoLink';

export default function Home() {
  return (
    <section>
      <p>Use the links on the left to navigate the examples.</p>
      <p>
        The code is in the <RepoLink />
      </p>
    </section>
  );
}
